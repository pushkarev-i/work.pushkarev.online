import { defineConfig } from 'astro/config';

// Статический сайт (мультиссылка + клиентский чат-виджет) — деплой на Vercel.
export default defineConfig({
  site: 'https://work.pushkarev.online',
});
